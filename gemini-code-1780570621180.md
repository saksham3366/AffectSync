You are an expert full-stack developer. I need you to build the complete Node.js/Express backend and MongoDB database for "AffectSync", integrating it with a Python Keras emotion detection model.

We are implementing a metadata-first approach to reduce AI load, strict gender-based color theory for outfit recommendations, and a rapid "regenerate" feature.

Please generate the complete, deployable code for the following requirements:

### 1. DATABASE SETUP & DUMMY LOGINS (MongoDB + Mongoose)
* **User Schema:** Create a User model with `username`, `password`, and `gender` (Enum: 'Male', 'Female').
* **Dummy Data Script:** Write a seed script that automatically creates a few dummy users upon database initialization (e.g., test_male / pass123 / Male, test_female / pass123 / Female) using JWT for authentication.
* **ClothingItem Schema:** Create a strict schema with:
    * `user_id` (ref User)
    * `image_url` (String)
    * `category` (Enum: 'top', 'bottom', 'layer', 'accessory')
    * `occasion` (Array of Enums: 'party', 'traditional', 'formal', 'casual')
    * `color_spectrum` (Array of Strings: 'dark', 'light', 'bright', 'monochrome', 'black', 'white', 'beige', 'pink', 'yellow', etc.)
    * `added_at` (Date)

### 2. THE UPLOAD PIPELINE (Pre-processing)
* Create a `POST /wardrobe/add` endpoint.
* Before processing the image, the request body MUST contain `category`, `occasion`, and `color_spectrum` tags. 
* Upload the image via Multer to Cloudinary (or a local uploads folder for now), and save the secure URL along with all metadata tags directly to MongoDB.

### 3. PYTHON KERAS INTEGRATION (The Trigger)
* Create a `POST /ai/detect` endpoint in Node.js.
* This endpoint receives webcam frames from the frontend, forwards them to the Python/Keras service via HTTP, and returns a single dominant emotion label (e.g., "happy", "sad", "neutral").

### 4. ADVANCED MOOD & GENDER RECOMMENDATION ENGINE
* Create a `POST /recommend-outfits` endpoint. It accepts the `emotion`, `user_id`, and an optional `exclude_ids` array (for the regenerate feature).
* **Logic:** Retrieve the user's gender from the DB. Generate **at least 3 distinct outfit combinations** (Top + Bottom + Layer) per request, strictly following these styling rules using Mongoose queries:

    **FOR MALES:**
    * **If Happy:** Query for [bright/light Top] + [light/dark Bottom] + [bold/printed Layer].
    * **If Sad/Neutral:** Query for a Monochrome fit. Match [dark Top] + [dark Bottom] + [dark Layer] OR all light.
    * **General Rule:** If Bottom is 'dark' (like dark jeans), Top MUST be 'light' and plain, and Layer MUST be 'bold/bright'.

    **FOR FEMALES:**
    * **If Happy:** Query for [light Bottom, e.g., beige/white] + [bright/pastel Top, e.g., pink/yellow].
    * **If Sad/Neutral:** Query for an All-Black or dark monochrome fit [black Top] + [black Bottom].

### 5. THE "REGENERATE" LOGIC (Single Click Refresh)
* To allow the user to reject a fit and get a new one instantly, the frontend will send back the `_id`s of the rejected clothing items in the `exclude_ids` array.
* The MongoDB query MUST use `$nin: exclude_ids` to ensure the exact same items are not shown again in the next generated batch.

### 6. API RESPONSE FORMAT
* The response must be perfectly structured for a React frontend to render easily.
* Return JSON like: 
    `{ success: true, mood: "happy", combinations: [ { outfit_1: { top: {...}, bottom: {...}, layer: {...} } }, ... ] }`

Write the full Express server setup, the Mongoose models, the auth middleware, and the complex aggregation/filtering logic for the recommendation endpoint.